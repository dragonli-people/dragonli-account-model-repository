package org.dragonli.service.modules.accountservice.entity.enums;

public enum BusinessStatus {
    INIT,
    FROM_BEGIN,/* 暂未用到 */
    FROM_FINISHED,/* 暂未用到 */
    TO_BEGIN,/* 暂未用到 */
    TO_FINISHED,/* 暂未用到 */
    SUCCESS,
    FAILED,
    MANUAL_HANDLED,/*暂未用到，且暂未更新到表中*/
}